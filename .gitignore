# local-only: running the trained model on the mac (weights, converter, venv)
mac/

# trained weights and checkpoints, far too big for git
*.safetensors

# caches
__pycache__/
.ipynb_checkpoints/
.DS_Store

# eval env
eval/.venv/
eval/run.log
eval/quick.py
eval/quick.log
